version: 2

# Define your raw data sources here
# This is where you tell dbt where your raw data lives in MySQL

sources:
  - name: raw
    description: Raw data tables in MySQL
    schema: dbt_mastery
    tables:
      - name: raw_flights_data
        description: Raw flight routes data loaded from CSV
